# Server Port
PORT=5000

# PostgreSQL Database Connection URL (e.g. Neon, Supabase, Render, or local PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kosal_crm?sslmode=require

# JWT Secret & Expiry
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_EXPIRES_IN=1d
